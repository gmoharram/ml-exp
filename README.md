# ml-exp
Repo Structure for Machine Learning Experiments with Hydra

# setup
(with uv)
```
python -m uv venv
python -m uv sync
source .venv/bin/activate # or equivalent
pip install -e . --no-deps # Install repo code as package (without reinstalling dependencies)
```
