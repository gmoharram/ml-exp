defaults:
  - _self_

run:
  dir: outputs/${now:%Y-%m-%d-%H-%M-%S}

job:
  chdir: true
  config:
    override_dirname:
      exclude_keys: []

sweep:
  dir: outputs/${now:%Y-%m-%d-%H-%M-%S}
  subdir: ${hydra.job.num}